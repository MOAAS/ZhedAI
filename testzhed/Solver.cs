using System;
using System.Collections.Generic;

namespace ZhedSolver
{
    class Solver {
        ZhedBoard board;

        public Solver(ZhedBoard board) {this.board = board;}

         public List<Tuple<Coords, Operations>> solve() {
            List<Tuple<Coords, Operations>> result = new List<Tuple<Coords, Operations>>();



            return result;
        } 
    }
}